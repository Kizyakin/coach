import {useEffect,useMemo,useState} from 'react'; import {api} from '../lib/api';
type O={eco:string;name_ru:string;name_en:string;moves:string;side:string;style:string[];difficulty:number;theory:number;elo_min:number;when:string;why:string;plan_white:string[];plan_black:string[];next:string[]};
export default function Openings(){const [list,setList]=useState<O[]>([]),[q,setQ]=useState(''),[active,setActive]=useState<O|null>(null);useEffect(()=>{api<O[]>('/api/openings').then(x=>{setList(x);setActive(x[0]||null)}).catch(()=>{})},[]);const filtered=useMemo(()=>list.filter(o=>(o.name_ru+o.name_en+o.eco).toLowerCase().includes(q.toLowerCase())),[q,list]);return <div className="openingLayout"><section><div className="sectionIntro"><h2>Энциклопедия дебютов</h2><p>ECO-каталог будет расширяться до полного дерева. Каждый вариант объясняется через смысл, планы и дальнейшие ходы.</p></div><input className="search" placeholder="Найти дебют, ECO или вариант…" value={q} onChange={e=>setQ(e.target.value)}/><div className="openingList">{filtered.map(o=><button key={o.eco+o.name_ru} className={active===o?'openingRow active':'openingRow'} onClick={()=>setActive(o)}><span>{o.eco}</span><div><b>{o.name_ru}</b><small>{o.name_en}</small></div><em>{o.side}</em></button>)}</div></section><aside className="detail card openingDetail">{active&&<><div className="tags"><span className="tag">{active.eco}</span>{active.style.map(s=><span className="tag muted" key={s}>{s}</span>)}</div><h2>{active.name_ru}</h2><small>{active.name_en}</small><div className="moves">{active.moves}</div><h4>Когда использовать</h4><p>{active.when}</p><h4>Зачем</h4><p>{active.why}</p><div className="split"><div><h4>План белых</h4><ul>{active.plan_white.map(x=><li key={x}>{x}</li>)}</ul></div><div><h4>План чёрных</h4><ul>{active.plan_black.map(x=><li key={x}>{x}</li>)}</ul></div></div><h4>Дальнейшие ходы</h4><div className="chips">{active.next.map(x=><button key={x}>{x}</button>)}</div><button className="primary">Тренировать этот дебют</button></>}</aside></div>}
